# Global Mobility AIOS — V12 Changelog Archive Through V12.33

This archive marker was created during the V12.34 documentation reconciliation so the formerly active `docs/CHANGELOG.md` history remains easy to locate in Git history without allowing the active changelog to grow indefinitely.

The complete pre-V12.34 active changelog is preserved immutably at Git object/blob:

```text
c9111991129cab34f5f236e85c9bc40df5bc3f59
```

and at the parent branch history immediately before the V12.34 changelog rotation. Git remains authoritative for the exact historical text.

Key most-recent entries before rotation were:

- V12.33 — bounded SecretsPort / non-production OpenBao pilot;
- V12.32 — supplemental blind Austria AI domain-corroboration harness;
- V12.31 — Technology Radar V1.3.5 external agent-infrastructure classification;
- V12.30 — L live-runtime acceptance evidence reconciliation;
- V12.29 — Hy4 Preview frontend-development Radar entry;
- V12.28 — repository-truth / L acceptance and CI reconciliation.

Older V12 history remains available through this branch's Git history and the existing archive documents referenced by the pre-rotation changelog.

This marker does not rewrite or supersede historical acceptance evidence; it only establishes the archive boundary used by the V12.34 active changelog.
