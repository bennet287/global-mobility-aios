# Global Mobility AIOS — V1.3-G.4.1 Acceptance Record

**Date:** 2026-08-20  
**Branch:** `roadmap/global-mobility-aios-v12`  
**Accepted implementation head:** `65ed83270389d5de88d917c5562077c1fbf9c8de`  
**Status:** COMPLETE / PASS / SEALED

## Accepted scope

V1.3-G.4.1 is the bounded Eligibility Vertical Contract Consolidation slice that hardens only the shared seams proven by the accepted E.2 → F.1 → G.1 → G.2 → G.3 → G.4 governed eligibility vertical.

It is behavior-preserving consolidation, not a new workflow, intent, effect, review, authority or runtime framework.

Permanent rule:

> **Consolidate proven meaning; do not generalize merely because code looks similar.**

## Accepted contracts

### Canonical mobility intent → domain mapping

Public service:

```text
app.services.mobility_domain.mobility_intent_domain(...)
```

Accepted first-slice mapping remains:

```text
study_abroad / study / student             → study
overseas_job / work / job / employment     → work
visa / permanent / residency / immigration → visa
anything else                               → general
```

E.2 and F.1 now consume the same public contract rather than duplicate string tables.

### System-bound AI employee command context

Public constructor:

```text
app.services.organization_command.system_bound_agent_command_context(...)
```

Accepted semantics remain:

```text
actor_id               = OrganizationPosition.position_key
actor_type             = agent
authenticated_user_id  = system
role                    = operator
position_key            = OrganizationPosition.position_key
```

This is an execution boundary only. It does not create Human authority, provider/model authority or CapabilityAuthority.

### Public pathway publication-integrity contract

Public service:

```text
app.services.pathway_publication_integrity.pathway_publication_integrity_blockers(...)
```

F.1 no longer imports the private `_publication_evidence_blockers` catalogue helper directly. The public adapter delegates to the accepted mature catalogue implementation so publication Evidence/rule/source semantics do not drift.

### Public eligibility action reconstruction contracts

G.2 now exposes and G.3 consumes:

```text
eligibility_command_context(...)
original_eligibility_attempt_payload(...)
rebuild_eligibility_action(...)
```

The previous private G.2 cross-slice imports are no longer part of G.3.

The reconstructed E.2 MaterialAction continues to preserve actor identity, capability, subject, profile-version precondition, idempotency, state, context/runtime/intent fingerprints, scope, Evidence/VerifiedRule basis, rationale, consequence semantics and trace identity.

## Explicitly deferred work

G.4.1 deliberately does not:

- replace `session.expire_all()` in E.2/G.1;
- create a generic reference/fingerprint resolver across E.2/F.1/G.1;
- introduce a `GenericEffectEngine`;
- introduce a universal intent bus;
- introduce a generic peer-review network;
- add reassessment/version-2 semantics;
- alter provider-egress policy;
- add a migration or schema change;
- change the accepted G.4 HTTP request/response boundary.

The existing conservative session refresh remains because a narrower freshness mechanism has not yet proven complete coverage of every hash-bearing ContextBundle dependency.

## Acceptance issue verified and repaired

The initial focused G.4.1 run produced one failure:

```text
test_g4_1_g3_consumes_public_g2_action_contracts
```

The implementation was verified correct. The test used the substring assertion:

```text
"_command_context" not in source
```

which incorrectly matched the intended public symbol:

```text
eligibility_command_context
```

This was classified as a **test defect**, not an implementation defect.

The test was repaired to inspect the module namespace for the exact forbidden private symbols. The repair touched only:

```text
apps/api/tests/test_organization_eligibility_contract_consolidation.py
```

The final accepted implementation head is therefore:

```text
65ed83270389d5de88d917c5562077c1fbf9c8de
```

## Canonical acceptance evidence

Human Owner local Windows acceptance evidence on the accepted implementation head:

```text
G.4.1 focused contract tests       7 passed / 1 warning / 0 failed
Pathway catalogue regression       2 passed / 1 warning / 0 failed
E.2 → G.4.1 governed vertical      88 passed / 1 warning / 0 failed
Repository policy                  PASS
Full API regression                1057 passed / 5 skipped / 1 warning / 0 failed
Full API duration                  400.30s
Database migration check           PASS
Migration head                     0077_canonical_eligibility_assessment_revision
Registered tables                  119
Physical schema                    ok
Database revision                  0077_canonical_eligibility_assessment_revision
Local DB schema                    PASS
Actual tables                      119
Physical tables                    120
Infrastructure tables              ["alembic_version"]
git diff --check                   clean
V12 branch                         clean / synchronized
```

Known non-blocking warning remains:

```text
StarletteDeprecationWarning:
Using httpx with starlette.testclient is deprecated; install httpx2 instead.
```

No dependency change is implied by this accepted slice.

## Database / migration truth

G.4.1 adds no model/table/migration.

Canonical database truth remains:

```text
migration head      0077_canonical_eligibility_assessment_revision
registered tables   119
actual tables       119
physical tables     120 including alembic_version
```

## Constitutional boundary preserved

G.4.1 changes code ownership boundaries only. It does not change authorization semantics.

Permanent invariants remain:

> **The Human Owner / Board is the supreme authority of Global Mobility AIOS; no agent, AI executive, model, runtime, tool, policy engine, or delegated authority may supersede it.**

> **Operational autonomy must never create organizational opacity.**

> **Scores route decisions; deterministic gates authorize decisions.**

> **Memory provides continuity. Evidence provides authority.**

## Repository state

At acceptance:

```text
V12 accepted implementation head
65ed83270389d5de88d917c5562077c1fbf9c8de

V11 frozen reference
ac130deaafa7aa44068e9459facbda2b4df327d6
```

V11 was reverified identical/frozen during this acceptance stream.

GitHub returned no attached combined-status checks for the accepted implementation head. Therefore no GitHub CI PASS is claimed.

## Acceptance decision

V1.3-G.4.1 is:

```text
COMPLETE / PASS / SEALED
```

The first governed eligibility vertical now has its accepted orchestration surface and its proven shared semantic seams consolidated without introducing a speculative horizontal framework.

## Next bounded stage

The next active implementation stage is:

```text
V1.3-G.5 — Eligibility Reassessment / Supersession
```

G.5 must first add an explicit expected canonical eligibility-revision precondition to the material-action contract before any revision v2+ effect is allowed.

Target semantic shape:

```text
accepted canonical revision v1 ACTIVE
→ new governed reassessment proposal
→ deterministic readiness
→ blind independent verification
→ verification floor
→ fresh Command Gateway authorization
→ create revision v2 ACTIVE
→ mark revision v1 SUPERSEDED
```

Prior canonical revision truth must remain append-only and inspectable. No in-place rewrite of historical canonical eligibility truth is permitted.
