# Global Mobility AIOS — Active V12 Product, Platform & High-Autonomy Roadmap

**Roadmap generation:** V12.23 — I.4 qualified + temporal autonomy evidence evaluation COMPLETE / PASS / SEALED; next autonomy-mutation design gate NOT STARTED
**Date:** 2026-08-22
**Active development branch:** `roadmap/global-mobility-aios-v12`
**V12 fork origin:** `dd2f2cd6e9e47179b1fd744ba3f56daf7c787449`
**Frozen V11 reference branch:** `roadmap/global-mobility-aios-v11`
**Final V11 documentation-cleanup head:** `ac130deaafa7aa44068e9459facbda2b4df327d6`
**Accepted product baseline:** Phase 13.16.10 — COMPLETE / PASS at `b8393d0b6bdaf02c958bb151b4909b5b82fd0d09`
**Active human-acceptance stream:** Phase 13.17 — owner-led genuine human acceptance — IN PROGRESS / PAUSED BY EVALUATOR
**Active organization architecture:** `GLOBAL_MOBILITY_AIOS_COMBINED_ARCHITECTURE_V1_1.md` + `HUMAN_LIKE_AGENT_ORGANIZATION_ARCHITECTURE_V1_3.md`
**Active Technology Radar:** `TECHNOLOGY_RADAR_V1_3_1.md`
**Context compression:** LLMLingua-2 — SELECTED PRIMARY PILOT behind AIOS-owned `ContextCompressionPort`; not production adopted
**Plasma donor programme:** Wiki 1.2.0 + Fractal 1.1.0 — PILOT APPROVED; PR #7 remains DRAFT / SOURCE IMPORT INCOMPLETE
**Munder donor baseline:** `v0.4.4` — strategic donor / controlled adoption programme
**Last accepted V1.3 checkpoint:** V1.3-I.4 — COMPLETE / PASS / SEALED on technical candidate `46727cd130923f4ede825965cea3a011537a930b`
**Latest accepted Production Proof:** GitHub Actions run `32560318311` — 4/4 jobs PASS for I.4
**Latest standalone Repository Policy:** GitHub Actions run `32560318310` — PASS for exact I.4 technical candidate
**Required-check enforcement:** CONFIGURED / OWNER-CONFIRMED — active `Production proof enforcement` ruleset on `main`
**H.2:** BOUNDED FOUNDATION CLOSED — accepted measurement + restrict-only safety foundation is sufficient for I-stage work; full future Immune System is not claimed complete
**V1.3-I.1:** COMPLETE / PASS / SEALED — canonical capability/context autonomy profile + deterministic evidence lineage + Board transparency foundation; no automatic promotion/downgrade
**V1.3-I.2:** COMPLETE / PASS / SEALED — immutable exact-profile shadow observations + deterministic Board evidence profile; no autonomy mutation
**V1.3-I.3:** COMPLETE / PASS / SEALED — exact-I.1-profile Board promotion criteria + deterministic eligibility read model; profile-precondition serialization hardening separately SEALED; no autonomy mutation or authority grant
**V1.3-I.4:** COMPLETE / PASS / SEALED — exact-profile, source-qualified, temporal and operationally bounded promotion-grade evidence evaluation; no autonomy mutation or authority grant
**Code migration head:** `0081_capability_autonomy_evidence_evaluation_policy`

<!-- CURRENT_MIGRATION_HEAD: 0081_capability_autonomy_evidence_evaluation_policy -->

> **V11 preserves the checkpoint. V12 proves and implements the direction.**

> **Governance before unrestricted execution. Transparency before increased autonomy. Production proof before additional safety architecture.**

---

## 1. What Global Mobility AIOS is

Global Mobility AIOS is being built as a **governed, evidence-grounded, transparent and cost-intelligent high-autonomy digital organization for global mobility**.

It is not merely an immigration chatbot, visa questionnaire, generic AI assistant, CRM with AI, document uploader, workflow engine, disconnected multi-agent demo, generic SaaS/admin surface, browser agent or human approval queue.

Target identity:

> **Global Mobility AIOS coordinates persistent AI employees to perform global-mobility work through dynamic Missions, purpose-scoped context, earned capability-specific autonomy, risk-tiered verification, governed execution and Human Owner / Board sovereignty.**

Operating principles:

> **AIOS does the work. Humans govern the exceptions and retained authority.**

> **The safety infrastructure exists to enable autonomy, not suppress it.**

> **Board by exception. Transparency by default.**

> **Agents are allowed to be wrong while thinking; AIOS is not allowed to be wrong silently when committing truth.**

> **Architectural sophistication must be matched by automated production proof.**

> **Quality first. Cost intelligence second. Premium compute only where it produces measurable additional value.**

> **No new major framework by default; prove a measured architectural gap first.**

Canonical architecture and acceptance records:

- `docs/GLOBAL_MOBILITY_AIOS_COMBINED_ARCHITECTURE_V1_1.md` — active canonical refinement
- `docs/HUMAN_LIKE_AGENT_ORGANIZATION_ARCHITECTURE_V1_3.md` — constitutional high-autonomy source
- `docs/GLOBAL_MOBILITY_AIOS_FINAL_COMBINED_ARCHITECTURE_V1.md` — historical predecessor
- `docs/TECHNOLOGY_RADAR_V1_3_1.md` — active technology/adoption direction
- `docs/V1_3_H2_FOUNDATION_CLOSURE_AND_I1_ENTRY_2026-08-21.md` — H→I stage decision
- `docs/V1_3_I1_CAPABILITY_AUTONOMY_PROFILE_FOUNDATION_2026-08-21.md`
- `docs/V1_3_I1_CAPABILITY_AUTONOMY_PROFILE_ACCEPTANCE_2026-08-21.md`
- `docs/V1_3_I2_SHADOW_AUTONOMY_EVIDENCE_PROFILE_FOUNDATION_2026-08-22.md`
- `docs/V1_3_I2_SHADOW_AUTONOMY_EVIDENCE_PROFILE_ACCEPTANCE_2026-08-22.md`
- `docs/V1_3_I3_AUTONOMY_PROMOTION_ELIGIBILITY_POLICY_FOUNDATION_2026-08-22.md`
- `docs/V1_3_I3_AUTONOMY_PROMOTION_ELIGIBILITY_POLICY_ACCEPTANCE_2026-08-22.md`
- `docs/V1_3_I3_PROFILE_PRECONDITION_HARDENING_ACCEPTANCE_2026-08-22.md`
- `docs/V1_3_I4_QUALIFIED_TEMPORAL_AUTONOMY_EVIDENCE_EVALUATION_FOUNDATION_2026-08-22.md`
- `docs/V1_3_I4_QUALIFIED_TEMPORAL_AUTONOMY_EVIDENCE_EVALUATION_ACCEPTANCE_2026-08-22.md`
- `docs/V12_22_PENDING_CHANGELOG.md` — historical filename; content closed as V12.22 I.3 acceptance changelog
- `docs/V12_23_PENDING_CHANGELOG.md` — historical filename; content closed as V12.23 I.4 acceptance changelog
- `docs/MUNDER_DIFFLIN_AIOS_ADOPTION_V1.md`
- `docs/PLASMA_AIOS_ADOPTION_V1.md`

---

## 2. Complete mobility lifecycle target

```text
Goal
→ profile / circumstances / constraints / consent
→ mobility strategy
→ country and pathway discovery
→ eligibility and alternatives
→ Evidence requirements and collection
→ official rules and regulatory intelligence
→ risk / cost / timeline / dependencies
→ documents and consistency
→ professional / regulated review where required
→ application preparation
→ Decision Readiness / verification
→ Human / Board authority where required
→ submission / appointment / external action
→ authority response
→ remediation / follow-up / appeal where applicable
→ relocation / post-arrival obligations
→ renewal / status change / family progression
→ long-term residence
→ citizenship / business / investment / long-term mobility strategy
```

The lifecycle must support changed goals, employers and jurisdictions; rejected applications; expired Evidence; superseded rules; family dependencies; long-lived case history; reassessment; and future mobility strategy.

---

## 3. Repository truth and accepted checkpoints

The roadmap distinguishes three states:

```text
IMPLEMENTED            code/docs exist
ACCEPTANCE PENDING     implementation has not yet satisfied its required proof gate
COMPLETE / PASS / SEALED
                       accepted evidence exists and repository truth is reconciled
```

Implementation is never treated as acceptance automatically.

Current V1.3 state:

| Stage | State | Meaning |
|---|---|---|
| V1.3-A Constitutional Contracts | COMPLETE / PASS / SEALED | Constitutional risk, authority, transparency and consequence primitives accepted |
| V1.3-B Minimal Governance Kernel | COMPLETE / PASS / SEALED | Bounded Command Gateway/governance kernel accepted |
| V1.3-C Transparency Foundation | COMPLETE / PASS / SEALED through C.4 | Board-inspectable activity/trace/read contracts accepted |
| V1.3-D Context + persistent employee/runtime | COMPLETE / PASS / SEALED through D.3 | OrganizationPosition, ContextBundle and runtime separation accepted |
| V1.3-E First Governed Mobility Vertical | COMPLETE / PASS / SEALED through E.2 | Governed mobility brief and eligibility intent accepted |
| V1.3-F Decision Readiness | COMPLETE / PASS / SEALED through F.1 | Deterministic readiness routing accepted |
| V1.3-G Independent Verification + canonical eligibility | COMPLETE / PASS / SEALED through G.5 | Blind verification, verification floor, canonical effect, orchestration and reassessment accepted |
| V1.3-H.1 Eligibility Immune Circuit | COMPLETE / PASS / SEALED | Restrictive aggregate circuit, canonical-lineage consolidation and proof accepted |
| V1.3-H.2.1 Eligibility Warning Recurrence Guard | COMPLETE / PASS / SEALED | Third verifier disagreement in one recovery epoch opens the exact aggregate circuit; PostgreSQL concurrency proof accepted |
| V1.3-H.2.2 Eligibility Runtime-Health Attribution | COMPLETE / PASS / SEALED | Trusted producer/verifier runtime identity is durably paired with runtime-health warnings; atomicity and identity-drift adversarial proof accepted |
| H.2.2 Runtime-Health Classification Refinement | COMPLETE / PASS / SEALED | Configuration/binding, transport and response-contract failures are distinguished; measurement-only |
| V1.3-H.2.3 Eligibility Revision-Conflict Attribution | COMPLETE / PASS / SEALED | Genuine pre-egress stale reassessments are durably attributed; no recurrence policy |
| V1.3-H.2.4 Post-Producer Revision-Race Attribution | COMPLETE / PASS / SEALED | Event-time revision-race attribution survives legitimate later supersession |
| V1.3-H.2 overall | BOUNDED FOUNDATION CLOSED | Accepted measurement + restrict-only safety foundation is sufficient for I-stage work; full future Immune System is not claimed |
| V12 Production Proof Gate | ACCEPTED / GREEN | Repository, backend SQLite, frontend and PostgreSQL lanes are continuously executable; latest accepted I.4 run `32560318311` |
| Required GitHub check enforcement | CONFIGURED / OWNER-CONFIRMED | Active `Production proof enforcement` ruleset protects `main` and requires all four Production Proof checks |
| V1.3-I.1 Capability-Specific Autonomy Profile + Evidence | COMPLETE / PASS / SEALED | Canonical capability/context autonomy truth and Board ceiling accepted on `581df5d99b65f0a7a49ace228ee707b881d508fa` |
| V1.3-I.2 Shadow Autonomy Evidence Profile | COMPLETE / PASS / SEALED | Immutable exact-profile measurements and deterministic Board evidence projection accepted on `c23e64a95770b1736ac9921486f8d017d17f930b` |
| V1.3-I.3 Promotion Eligibility Policy | COMPLETE / PASS / SEALED | Board-authored policy is frozen to the exact I.1 profile revision and deterministically evaluates I.2 evidence without mutating autonomy; foundation accepted on `77b2e9adb30d69419158930b31c0bc10515cb6a7`, profile-precondition hardening sealed on `108231d75b4c7413c1759c003e121fdcca206d7c` |
| V1.3-I.4 Qualified + Temporal Evidence Evaluation | COMPLETE / PASS / SEALED | Exact-profile, canonical-source-qualified, temporally bounded promotion-grade evidence evaluation accepted on `46727cd130923f4ede825965cea3a011537a930b`; no autonomy mutation |

The accepted V1.3 baseline is now **I.4**, sealed by `docs/V1_3_I4_QUALIFIED_TEMPORAL_AUTONOMY_EVIDENCE_EVALUATION_ACCEPTANCE_2026-08-22.md` on exact technical candidate `46727cd130923f4ede825965cea3a011537a930b`, Production Proof run `32560318311`.

I.1, I.2 and I.3 remain sealed parent checkpoints. The later I.3 command-concurrency refinement remains separately sealed by `docs/V1_3_I3_PROFILE_PRECONDITION_HARDENING_ACCEPTANCE_2026-08-22.md` on candidate `108231d75b4c7413c1759c003e121fdcca206d7c`, Production Proof run `32539026789`.

I.3 adds policy/eligibility truth only. `ELIGIBLE` is evidence satisfaction, not permission. I.4 adds stricter promotion-grade evidence qualification and temporal/bounded evaluation, but it also remains non-authorizing.

No actual autonomy-change mechanism is accepted or implemented.

A sequencing constraint remains explicit: **the project must not jump directly from evidence eligibility into autonomous self-promotion.** A later capability-specific mutation increment requires a separate bounded design and proof covering Board-only authority, exact current-profile serialization, I.3 policy applicability, I.4 promotion-grade readiness, Board ceilings, append-only change lineage, Human override/recovery behavior and real PostgreSQL races.

---

## 4. Historical compatibility contract — protected

The active V12 roadmap preserves historical markers protected by repository regression tests and meaningful to current Evidence provenance.

`v10.22` introduced **multi-batch tranche operations** around the governed jurisdiction Evidence workflow.

Historical database lineage includes:

```text
0032_initial_rule_assertions
```

Protected exact markers:

```text
v10.22
multi-batch tranche operations
0032_initial_rule_assertions
```

No V1.3, canonical eligibility-effect, Immune System, CI or Earned Autonomy work may erase this provenance contract.

---

## 5. Latest accepted quality evidence — I.4

Historical accepted results are not represented as rerun unless actually rerun.

The latest accepted technical checkpoint is I.4:

```text
I.4 technical candidate                           46727cd130923f4ede825965cea3a011537a930b
GitHub Production Proof run                       32560318311 — 4/4 jobs PASS
Standalone Repository Policy run                  32560318310 — PASS
Python                                             3.12.14
Alembic                                            PASS — 0001 → 0081
Registered SQLModel application tables             124
Fresh SQLite physical tables                       125 including alembic_version
Fresh PostgreSQL 16 schema                         PASS
Backend SQLite regression                         1172 passed / 19 skipped / 1 warning / 0 failed
PostgreSQL governed/autonomy suite                  102 passed / 1 warning / 0 failed
I.4 exact-profile evaluation-policy binding        PASS
I.4 current-profile PostgreSQL serialization       PASS
I.4 qualification adapter                          PASS — eligibility.proposal only
I.4 canonical eligibility-lineage validation       PASS
I.4 raw I.2 attestation disagreement tests         PASS
I.4 observation/source age separation              PASS
I.4 old-source/new-observation non-refresh          PASS
I.4 future-dated evidence fail-closed behavior      PASS
I.4 bounded candidate count                         PASS
I.4 explicit terminal Human review derivation       PASS
I.4 ambiguous review ordering fail-closed behavior  PASS
I.4 bounded summary transparency                    PASS
I.4 stable capped cursor provenance                 PASS
I.4 no HTTP mutation route                          PASS
I.4 no autonomy/Board-ceiling/authority mutation    PASS
I.4 PostgreSQL profile-supersession race            PASS
Frontend npm ci                                     PASS
Frontend high-severity audit                        PASS
Frontend design foundation                          PASS
Frontend request/auth                               PASS
Frontend TypeScript                                 PASS
Frontend Next.js production build                   PASS
Frontend compiled auth                              PASS
Repository policy                                   PASS
Release consistency                                 PASS — 0081
Python dependency constraints                       PASS
Diff hygiene                                        PASS
```

The known Pydantic 2.8 `model_metadata_json` protected-namespace warning remains visible and non-blocking.

The final I.4 repair was deliberately test-boundary-only: SQLite-loaded `OrganizationHumanAction.occurred_at` values were normalized to aware UTC before tests constructed explicit `evaluation_as_of` values. The production evaluator still rejects timezone-naive explicit evaluation timestamps.

### 5.1 Diagnostic I.3 candidate retained as negative evidence

The unaccepted pre-repair I.3 candidate:

```text
07260260a442418589f76e3df154a35bb2557e96
```

produced exact GitHub backend result:

```text
7 failed / 1146 passed / 15 skipped / 1 warning
```

The failure set revealed:

1. invalid physical `OrganizationActivityClass('authority')` in the I.3 policy writer;
2. stale-policy reuse risk because policy was not tied to exact I.1 profile identity;
3. a router-registry hardening count still expecting 66 rather than the I.3-correct 67.

The accepted I.3 foundation repair remains:

```text
77b2e9adb30d69419158930b31c0bc10515cb6a7  fix: bind I.3 policy to exact autonomy profile
```

This diagnostic history must not be represented as an accepted candidate.

### 5.2 I.3 profile-precondition hardening — retained sealed parent

A later adversarial review identified a narrower transaction race: exact-profile binding made a stale policy inert, but a Board command could still report success for a profile that became historical during the transaction.

The sealed refinement added caller `expected_profile_id` preconditioning and serialized I.3 policy establishment with I.1 profile supersession on the exact current profile row. PostgreSQL locks that row with `FOR UPDATE`, reruns canonical current-profile validation after the lock, revalidates autonomy/evidence-version/Board-ceiling semantics, and performs a final current-profile recheck before commit.

The real PostgreSQL contract deliberately makes I.1 supersession win the v1 row lock, proves the I.3 writer waits, commits v2, then requires the waiting writer to reject v1 as stale and persist no policy for the historical command attempt.

Canonical hardening record:

```text
docs/V1_3_I3_PROFILE_PRECONDITION_HARDENING_ACCEPTANCE_2026-08-22.md
```

I.4 reuses this exact current-profile concurrency doctrine for evaluation-policy establishment.

Repository-owner Settings evidence confirms that `main` is protected by an active ruleset requiring the four Production Proof checks before integration. The GitHub connector does not expose ruleset configuration directly, so this enforcement claim remains owner-confirmed rather than connector-introspected.

---

## 6. Permanent constitutional execution invariants

```text
Capability ≠ Authority ≠ Autonomy ≠ Risk
CAN DO ≠ MAY DO
Scores route; deterministic gates authorize.
Memory provides continuity. Evidence provides authority.
Conversation does not create authority.
Provider output does not become canonical truth automatically.
Compression output is derived context, not source truth.
Retrieved knowledge is data, not executable authority.
Model capability eligibility is measured, not self-declared.
Child delegation may narrow parent scope, never expand it.
Immune System is restrict-only and never grants authority.
Promotion eligibility is not permission.
Qualified evidence is not permission.
```

Human Owner / Board remains supreme authority.

Autonomy remains capability-specific:

| Level | Meaning |
|---|---|
| A0 | Prohibited |
| A1 | Human executes |
| A2 | AI prepares; approval required |
| A3 | Autonomous with mandatory review |
| A4 | Autonomous with monitoring and valid recovery controls |
| A5 | Fully autonomous bounded operation |

Risk remains consequence-oriented:

| Risk | Typical work | Verification |
|---|---|---|
| R0 | brainstorming / summarization | single agent |
| R1 | routine internal operation | deterministic checks |
| R2 | client-facing preparation / Evidence validation | Evidence validation |
| R3 | eligibility / material recommendation | blind independent verification |
| R4 | certification / regulatory publication | independent verification + fresh source + authority |
| R5 | government submission / critical reserved action | full AI preparation + required Human/Board authority |

---

## 7. Accepted governed eligibility vertical

The accepted R3 eligibility chain remains:

```text
trusted G.4 organization request / WorkItems
→ E.2 material proposal / governance attempt
→ F.1 deterministic Decision Readiness
→ G.1 blind independent verification
→ G.2 verification-floor re-evaluation
→ G.3 canonical governance authorization
→ G.3 semantic EligibilityAssessment effect
→ G.4 durable trace/effect identifiers
→ G.5 explicit canonical revision precondition for reassessment
→ append-only supersession lineage
```

Accepted aggregate identity:

```text
eligibility:<tenant_key>:<lead_id>:<pathway_id>
```

G.5 preserves two separate concurrency contracts:

```text
MaterialAction.expected_version
    = Profile.profile_version

expected_eligibility_revision_version
    = current ACTIVE canonical eligibility revision
```

There is no implicit reassessment and no last-write-wins canonical truth.

Historical replay may resolve already-durable superseded revisions but may not authorize new work from stale state.

The governed HTTP route remains:

```text
POST /api/v1/organization/eligibility/orchestrate
```

Request JSON cannot select tenant authority, OrganizationPosition, provider/model, autonomy, risk, scope or `CapabilityAuthority`.

---

## 8. Accepted H.1 Eligibility Immune Circuit Foundation

Canonical design record:

```text
docs/V1_3_H1_ELIGIBILITY_IMMUNE_CIRCUIT_FOUNDATION.md
```

H.1 is aggregate-scoped and uses existing `OrganizationActivity` durability rather than a new incident table.

Current control types:

```text
organization.immune.eligibility_incident.v1
organization.immune.eligibility_circuit_opened.v1
organization.immune.eligibility_circuit_closed.v1
```

Critical structural classes:

```text
CANONICAL_AGGREGATE_INTEGRITY
DURABLE_LINEAGE_INTEGRITY
```

Warning classes currently include:

```text
RUNTIME_HEALTH_FAILURE
REVISION_CONFLICT
VERIFIER_DISAGREEMENT
REASSESSMENT_ROLLBACK
```

Automatic opening is restrict-only. Recovery requires an authenticated human admin and restores execution attempts only; it grants no authority.

Fresh G.4 execution performs circuit/lineage preflight before provider egress. Exact committed historical replay remains provider-free but must satisfy durable canonical lineage.

---

## 9. H.1 canonical-lineage blocker — resolved and accepted

The repository has one domain-specific canonical eligibility-lineage contract:

```text
apps/api/app/services/organization_eligibility_lineage.py
```

Consumers now include:

```text
G.3 canonical-effect replay
G.4 orchestration replay
H.1 aggregate preflight
I.4 governed-eligibility promotion-grade source qualification
```

The shared validator proves existence, tenant, stable aggregate identity, revision/lifecycle/supersession, assessment identity, exact G.1/G.2/semantic Activity types, governance record kind, MATERIAL classification, source identity/version, action/readiness/verification/floor/effect fingerprints, expected revision identity/version, semantic revision/effect identity and E.2 → G.1 → G.2 → G.3 causation.

Aggregate validation requires one contiguous revision chain with exactly one latest ACTIVE revision.

This contract remains **ACCEPTED / SEALED** and is intentionally reused rather than partially reimplemented by I.4.

---

## 10. Adversarial H-stage regression — accepted

The H-stage acceptance surface intentionally corrupts committed lineage rather than testing only valid fixtures.

Representative corruptions include:

```text
verification Activity type
verification-floor Activity type
governance record kind
semantic Activity type
assessment/revision identity
semantic source revision identity
missing semantic lineage
invalid revision lifecycle
```

Required fresh-execution behavior remains:

```text
canonical validator rejects corruption
→ CRITICAL incident where applicable
→ exact aggregate circuit OPEN for accepted structural classes
→ producer calls = 0
→ verifier calls = 0
```

G.3/G.4 replay and I.4 source qualification fail closed on the same canonical invariant.

---

## 11. Production Proof Gate — accepted / continuously reused

CI workflow:

```text
.github/workflows/v12-production-proof.yml
```

Accepted proof lineage includes:

```text
H.1    0b19d61a417de2d372e101d4e132a6a0a6c2a84f  run 32463849415
H.2.1  9e63c358b9692529278595201250c4dc8bb1ff47  run 32469756908
H.2.2  c5c2a68ac3a9caf2551204d61862b6ad0b6281eb  run 32473526874
H.2.3  17edeca46af2b9cc7e0a6111ec2b3270f4bb1283  run 32480405051
H.2.4  e7584b90fc967e828960ae0730a35d8646fba74f  run 32500438187
I.1    581df5d99b65f0a7a49ace228ee707b881d508fa  run 32529241957
I.2    c23e64a95770b1736ac9921486f8d017d17f930b  run 32533230630
I.3    77b2e9adb30d69419158930b31c0bc10515cb6a7  run 32536826352
I.3-H  108231d75b4c7413c1759c003e121fdcca206d7c  run 32539026789  profile-precondition hardening
I.4    46727cd130923f4ede825965cea3a011537a930b  run 32560318311  qualified + temporal evidence evaluation
```

### 11.1 Repository policy / dependency contract

```text
repository policy
release consistency
Python direct-dependency constraints
diff hygiene
```

Candidate proof uses exact-head checkout. Candidate-local diff proof must use `git diff --check HEAD^`; plain `git diff --check` only inspects uncommitted working-tree changes.

### 11.2 Full backend regression — SQLite

The latest accepted I.4 proof runs:

```text
constrained Python dependency install
compileall
Alembic upgrade on isolated SQLite
full apps/api/tests pytest regression
migration head + physical schema verification
local schema contract
```

Latest accepted result:

```text
1172 passed / 19 skipped / 1 warning / 0 failed
```

SQLite is the broad regression environment, not a substitute for PostgreSQL production-database proof.

### 11.3 Frontend proof

Accepted frontend baseline remains:

```text
next=16.3.1
react=19.0.8
react-dom=19.0.8
```

Accepted proof includes:

```text
npm ci
npm audit --audit-level=high
design-foundation tests
request/auth tests
TypeScript --noEmit
Next.js production build
compiled-auth verification
```

The GitHub frontend lane uses Node 24.

There is not yet a Playwright/browser workflow suite. A browser golden journey remains required before aggressive frontend restructuring.

### 11.4 PostgreSQL governance proof

A real PostgreSQL 16 service is migrated through the single Alembic head, checked against SQLModel metadata, then used by focused governance/autonomy tests.

I.1 added competing-profile and stale-supersession proofs. I.2 added concurrent same-profile/same-source observation exclusion. I.3 added competing initial promotion-policy writers and stale cross-session policy supersession. I.3 hardening proved policy establishment serializes with I.1 profile supersession on the exact profile row. I.4 extends this discipline to evaluation-policy establishment and proves profile-supersession-wins stale-policy rejection while also exercising governed evidence qualification.

Latest accepted PostgreSQL evidence:

```text
Alembic 0001 → 0081                  PASS
registered application tables        124
physical schema                       PASS
governed eligibility/autonomy suite  102 passed / 1 warning / 0 failed
I.4 profile-supersession lock race    PASS
```

---

## 12. Python dependency reproducibility

Backend direct dependencies remain declared in:

```text
apps/api/requirements.txt
```

Exact direct-dependency constraints live in:

```text
apps/api/constraints.txt
```

Required constrained install:

```text
python -m pip install -r apps/api/requirements.txt -c apps/api/constraints.txt
```

The API Docker image and V12 Production Proof workflow use the same constraints.

Current claim boundary:

> **This is a direct-dependency reproducibility baseline, not a complete transitive lock.**

---

## 13. Migration/data-model doctrine

The latest accepted schema checkpoint is I.4 at:

```text
0081_capability_autonomy_evidence_evaluation_policy
```

I.1 adds:

```text
capability_autonomy_profiles
capability_autonomy_evidence
```

I.2 adds:

```text
capability_autonomy_evidence_observations
```

I.3 adds:

```text
capability_autonomy_promotion_policies
```

I.4 adds:

```text
capability_autonomy_evidence_evaluation_policies
```

Accepted I.4 schema:

```text
migration head                        0081
registered application tables         124
physical tables                        125 including alembic_version
fresh SQLite schema                    PASS
fresh PostgreSQL 16 schema             PASS
```

The I.3 profile-precondition hardening changed writer concurrency semantics only; it added no migration or table.

The migration doctrine remains:

> **Split model modules by bounded context; retain one SQLModel metadata registry and one controlled linear Alembic lineage/head.**

Do not create independent migration heads merely to mirror code-module boundaries.

Migration `0080` was corrected in place before acceptance because its earlier shape had never been accepted/sealed. I.4 legitimately advanced the lineage to `0081_capability_autonomy_evidence_evaluation_policy`; that migration is now accepted/sealed on exact candidate `46727cd130923f4ede825965cea3a011537a930b`.

---

## 14. Complexity/decomposition programme — sequenced, not simultaneous

Large-module concentration is accepted as a maintainability risk. It is not a reason to begin broad simultaneous refactors.

Known concentration areas include:

```text
organization_governance.py
domain.py
apps/web/lib/api.ts
apps/web/app/globals.css
```

### 14.1 Backend model decomposition

Extract bounded-context model modules while preserving one metadata registry and one migration lineage.

I.1 uses `app/models/autonomy_profile.py`; I.2 uses `app/models/autonomy_evidence_profile.py`; I.3 uses `app/models/autonomy_promotion_policy.py`; I.4 uses `app/models/autonomy_evidence_evaluation_policy.py`.

### 14.2 Frontend API decomposition

Split domain clients behind one common request/auth/error primitive. Do not create duplicate fetch/auth frameworks.

### 14.3 CSS decomposition

Move design tokens, primitives, layouts and feature styles incrementally. Preserve visual behavior through build/tests and future browser golden journeys.

### 14.4 Governance-service decomposition

Extract only semantic seams with stable contracts and tests. Do not split by arbitrary line-count quotas.

Permanent rule:

> **Improve regression detection before increasing refactor surface area.**

---

## 15. H.1 through H.2.4 acceptance gates — satisfied

H.1, H.2.1, H.2.2, H.2.3 and H.2.4 remain **COMPLETE / PASS / SEALED** under their individual acceptance records.

H.2.1 proves recurrence thresholding and exact aggregate circuit opening.

H.2.2 proves trusted runtime-health attribution and atomicity without provider-health policy.

The H.2.2 classification refinement separates:

```text
configuration_or_binding_failure      provider_egress_occurred=false
provider_transport_failure            provider_egress_occurred=true
provider_response_contract_failure    provider_egress_occurred=true
```

H.2.3 proves pre-egress revision-conflict attribution without automatic retry/rebase.

H.2.4 proves post-producer revision-race attribution while stale output remains prohibited from canonical effect.

The bounded H.2 foundation is closed. It may be reopened only by a separately measured failure model with explicit scope/window/blast-radius/recovery semantics.

---

## 16. Immediate execution order

The current H→I stage sequence is:

```text
1. H.1 canonical lineage + circuit foundation             COMPLETE / SEALED
2. Production Proof + required-check enforcement          COMPLETE
3. H.2.1 verifier-disagreement recurrence guard           COMPLETE / SEALED
4. H.2.2 runtime-health attribution foundation            COMPLETE / SEALED
5. H.2.3 revision-conflict attribution foundation         COMPLETE / SEALED
6. H.2.4 post-producer revision-race attribution          COMPLETE / SEALED
7. H.2.2 runtime-health classification refinement         COMPLETE / SEALED
8. H.2 bounded safety/measurement foundation              CLOSED
9. V1.3-I.1 autonomy profile + evidence foundation        COMPLETE / PASS / SEALED
10. I.1 exact-candidate Production Proof                  COMPLETE / PASS — run 32529241957
11. V1.3-I.2 shadow autonomy evidence profile             COMPLETE / PASS / SEALED
12. I.2 exact-candidate V12 Production Proof              COMPLETE / PASS — run 32533230630
13. V1.3-I.3 promotion eligibility policy foundation      COMPLETE / PASS / SEALED
14. I.3 exact-candidate V12 Production Proof              COMPLETE / PASS — run 32536826352
15. I.3 profile-precondition hardening                    COMPLETE / PASS / SEALED — run 32539026789
16. I.4 qualified + temporal evidence evaluation          COMPLETE / PASS / SEALED
17. I.4 exact-candidate V12 Production Proof              COMPLETE / PASS — run 32560318311
18. Actual capability promotion/demotion mutation design  NOT STARTED
19. Autonomy-change lineage / recovery / Human override   NOT STARTED
20. Mutation implementation and Production Proof          NOT STARTED / NOT PRE-AUTHORIZED
```

Browser golden-journey proof and incremental semantic decomposition remain parallel proof/maintainability priorities. They do not pre-authorize later autonomy mutation.

---

## 17. V1.3-H.2 — bounded safety/measurement foundation closed

H.2 remains closed as the accepted bounded safety/measurement foundation required to enter I-stage work. This does not claim the complete future Organizational Immune System is implemented.

Future H-stage reopening requires a newly measured failure model and explicit:

```text
measurement scope
time/window or recovery epoch
minimum evidence/sample contract
restriction blast radius
false-positive containment
human/automatic recovery boundary
interaction with existing aggregate circuit
authority/autonomy non-effect
```

Potential later work may include rolling-window anomaly policy, incident aggregation, bounded root-cause classification, provider/runtime health scoring and scope-limited blast-radius controls. None are pre-authorized.

---

## 18. V1.3-I — Earned Autonomy

### 18.1 I.1 — accepted canonical autonomy truth

**I.1 FOUNDATION IS COMPLETE / PASS / SEALED.**

Accepted technical candidate and proof:

```text
candidate = 581df5d99b65f0a7a49ace228ee707b881d508fa
run       = 32529241957
```

I.1 creates canonical append-only autonomy truth at exact tenant + persistent OrganizationPosition + capability + context scope + evidence-policy version.

Accepted records:

```text
CapabilityAutonomyProfile
CapabilityAutonomyEvidence
```

Accepted invariants include explicit A0–A5 autonomy, Board ceiling, separate authority/risk ceilings, append-only profile supersession, deterministic Activity evidence lineage, idempotency, expected profile sequence and Board-only canonical writer.

Board read surface:

```text
GET /api/v1/organization/transparency/autonomy/profiles/{position_key}/{capability_key}?context_scope=...
```

I.1 does not automatically promote or downgrade agents.

### 18.2 I.2 — accepted shadow autonomy evidence profile foundation

**I.2 IS COMPLETE / PASS / SEALED.**

Accepted technical candidate and proof:

```text
candidate = c23e64a95770b1736ac9921486f8d017d17f930b
run       = 32533230630
```

I.2 adds one append-only measurement record:

```text
CapabilityAutonomyEvidenceObservation
```

Every observation is frozen to an exact I.1 profile and a canonical source `OrganizationActivity` identity/fingerprint.

Trusted writers are Human Board/admin or trusted server SYSTEM measurement context. AGENT, WORKER and EXTERNAL_HUMAN actors cannot self-grade canonical evidence.

The historical I.2 projection derives execution volume, grounding, Human review outcomes, contradiction, compliance, freshness, critical errors, recovery, SLA and incident metrics with explicit denominators and `null` for undefined rates. Those remain immutable measurement facts; I.4 does not silently upgrade them into promotion-grade truth without canonical qualification.

Board read surface:

```text
GET /api/v1/organization/transparency/autonomy/profiles/{position_key}/{capability_key}/evidence?context_scope=...
```

I.2 is measurement only and does not mutate autonomy.

### 18.3 I.3 — accepted promotion eligibility policy foundation

**I.3 IS COMPLETE / PASS / SEALED.**

Accepted records:

```text
docs/V1_3_I3_AUTONOMY_PROMOTION_ELIGIBILITY_POLICY_FOUNDATION_2026-08-22.md
docs/V1_3_I3_AUTONOMY_PROMOTION_ELIGIBILITY_POLICY_ACCEPTANCE_2026-08-22.md
docs/V1_3_I3_PROFILE_PRECONDITION_HARDENING_ACCEPTANCE_2026-08-22.md
docs/V12_22_PENDING_CHANGELOG.md
```

Accepted foundation candidate and proof:

```text
candidate = 77b2e9adb30d69419158930b31c0bc10515cb6a7
run       = 32536826352
```

Accepted profile-precondition hardening candidate and proof:

```text
candidate = 108231d75b4c7413c1759c003e121fdcca206d7c
run       = 32539026789
```

I.3 adds one append-only Board policy record:

```text
CapabilityAutonomyPromotionPolicy
```

The accepted canonical policy is bound to the **exact immutable I.1 profile revision**:

```text
tenant
+ profile_id
+ profile_sequence witness
+ profile_record_fingerprint witness
+ persistent OrganizationPosition
+ capability
+ context scope
+ current autonomy level
+ exactly-next autonomy level
+ evidence-policy version
```

This exact-profile binding is required because a same-level I.1 supersession may change authority requirement, risk ceiling, Board ceiling or other profile semantics while retaining the same autonomy level/evidence-policy label. Such a new profile does not inherit the old policy.

The internal writer is Human Board/admin only, requires a one-level target and rejects targets above the current Board ceiling.

The hardening adds caller `expected_profile_id` preconditioning and serializes every new policy write with I.1 supersession using the exact current profile row lock plus canonical post-lock revalidation.

Accepted Activity representation:

```text
physical activity_class = decision
constitutional_activity_class = AUTHORITY
```

Policy revisions are append-only and protected by idempotency, expected policy sequence, exact-profile uniqueness, semantic fingerprint, captured profile fingerprint, Board Activity fingerprint and PostgreSQL concurrency controls.

The evaluator returns:

```text
ELIGIBLE
INSUFFICIENT_EVIDENCE
HOLD
```

`ELIGIBLE` is not permission. It does not mutate `CapabilityAutonomyProfile`, Board ceiling, authority/risk ceilings, `CapabilityAuthority`, Command Gateway decisions or external state.

Board read surface:

```text
GET /api/v1/organization/transparency/autonomy/profiles/{position_key}/{capability_key}/promotion-eligibility?context_scope=...
```

There is no I.3 HTTP mutation route.

### 18.4 I.4 — accepted qualified + temporal autonomy evidence evaluation

**I.4 IS COMPLETE / PASS / SEALED.**

Canonical records:

```text
docs/V1_3_I4_QUALIFIED_TEMPORAL_AUTONOMY_EVIDENCE_EVALUATION_FOUNDATION_2026-08-22.md
docs/V1_3_I4_QUALIFIED_TEMPORAL_AUTONOMY_EVIDENCE_EVALUATION_ACCEPTANCE_2026-08-22.md
docs/V12_23_PENDING_CHANGELOG.md
```

Accepted technical candidate and proof:

```text
candidate = 46727cd130923f4ede825965cea3a011537a930b
run       = 32560318311
policy    = 32560318310
```

I.4 adds one append-only Board evaluation-policy record:

```text
CapabilityAutonomyEvidenceEvaluationPolicy
```

The policy is bound to one exact immutable I.1 profile revision and uses I.3-hardened current-profile serialization. It owns the qualification contract, maximum observation age, maximum canonical source age and maximum candidate count. I.3 remains owner of promotion-quality thresholds.

The first and only accepted promotion-grade qualification adapter is intentionally capability-specific:

```text
capability = eligibility.proposal
qualification_contract = governed-eligibility-canonical-effect.v1
source semantic effect = organization.eligibility.assessment_committed.v1
```

Qualification reuses the accepted canonical E.2 → F.1 → G.1 → G.2 → G.3 eligibility-lineage validator. An arbitrary same-tenant Activity is not promotion-grade merely because a trusted I.2 writer referenced it.

For the supported adapter, I.4 derives only facts proven by canonical domain truth. Governed eligibility lineage proves qualifying execution, Evidence/rule grounding, non-contradictory accepted verification and execution-policy compliance. Human outcome is derived only from exact-revision immutable `OrganizationHumanAction` terminal actions:

```text
approved           → accepted
requested_changes  → modified
rejected           → rejected
no qualifying terminal action by evaluation_as_of → not_reviewed
```

Generic `reviewed`, attested, acknowledged, assigned or resolved actions do not imply acceptance. Ambiguous equal-time conflicting terminal outcomes fail closed.

Dimensions without exact typed canonical linkage remain unavailable rather than inherited from raw I.2 attestations. In I.4 v1 these include execution-specific source-freshness quality, critical-error outcome, recovery result, SLA and incident attribution where exact linkage is absent.

Temporal evaluation is explicit and strict:

```text
observation age = evaluation_as_of - observation.created_at
source age      = evaluation_as_of - source Activity.occurred_at
```

Both are separately bounded. New observation writes cannot refresh old canonical sources. Future-dated evidence fails closed. Explicit internal `evaluation_as_of` must be timezone-aware; Board HTTP transparency uses server time.

Accepted Board read surfaces:

```text
GET /api/v1/organization/transparency/autonomy/profiles/{position_key}/{capability_key}/evidence-evaluation?context_scope=...

GET /api/v1/organization/transparency/autonomy/profiles/{position_key}/{capability_key}/evidence-evaluation/provenance?context_scope=...&limit=...&cursor=...
```

The summary does not nest unbounded lifetime observations. Provenance is stable/capped cursor pagination. Candidate overflow fails closed rather than silently truncating metrics. Raw Activity payload JSON is not exposed.

There is no I.4 HTTP mutation route and no autonomy mutation.

Accepted proof:

```text
SQLite regression                    1172 passed / 19 skipped / 1 warning / 0 failed
PostgreSQL governed/autonomy suite   102 passed / 1 warning / 0 failed
Alembic                               0001 → 0081 PASS
registered application tables        124
physical tables                      125 including alembic_version
frontend                              PASS
repository/release/deps/diff          PASS
```

I.4 closes the qualified/temporal/bounded evidence prerequisite. It does **not** pre-authorize a promotion/demotion command.

---

## 19. Organization Fabric and Munder adoption

The future Organization Fabric includes persistent employee/runtime binding, organizational communication, Missions/WorkItems, Dynamic Mission Squads, presence/heartbeats, Mission Rooms, Skills/Capability Registry, memory mechanics, Event Nervous System, scheduling/triggers/webhooks and runtime telemetry.

Munder v0.4.4 remains a controlled donor. AIOS retains exclusive ownership of organizational semantics, authority, Evidence, risk, Decision Readiness, Command Gateway decisions and canonical state.

High-value donor areas remain runtime/provider abstraction, messaging, Skills, task coordination, circuit-breaking mechanics, triggers, memory, telemetry, token/cost tracking and live-scene concepts.

Hard rejects remain:

```text
SQLite/file state as canonical production authority
GOD-style unlimited implicit authority
direct material mutation bypassing AIOS governance
provider-owned organizational semantics
retro pixel-office UI as final product design
```

---

## 20. Transparency / Cockpit direction

Board Transparency remains a cross-cutting architectural invariant rather than a later dashboard feature.

Target drill-down:

```text
Organization
→ Department
→ Mission
→ Case
→ WorkItem
→ Squad
→ Agent
→ Conversation
→ Decision
→ Tool Action
→ Evidence / Rule
→ Canonical Effect
→ Incident / Recovery
→ Outcome
```

Board visibility does not imply Board interruption.

The top-level Human Owner / Board experience remains the **Global Mobility AIOS Cockpit**; Board Room is a module inside that control surface.

Accepted I.1 provides autonomy-profile history. Accepted I.2 provides immutable measurement evidence. Accepted I.3 provides criterion-by-criterion promotion eligibility while preserving no autonomy mutation path. Accepted I.4 provides source-qualified, temporal and bounded promotion-grade evaluation plus capped provenance.

Future mutation/change-lineage transparency must fit this same Cockpit model and must not create a second hidden autonomy truth.

---

## 21. Operational proof still to add after this gate

The V12 Production Proof Gate is the minimum continuous proof boundary, not the endpoint of operational maturity.

Still required later:

- Playwright/browser golden journey;
- controlled failure/recovery drills;
- backup/restore proof;
- observability SLOs and alert routing;
- load/concurrency benchmarks beyond focused canonical eligibility/autonomy contracts;
- security/dependency scanning policy beyond the accepted npm high-severity gate;
- production deployment rehearsal;
- provider outage/degradation drills;
- database restore and migration rollback/forward-recovery evidence;
- long-lived cost/latency/quality telemetry;
- query-driven autonomy evidence index tuning based on measured query plans/workload;
- production-scale evaluation/provenance load tests before broad capability expansion.

These should be added based on production risk, not to maximize tooling count.

The bounded/paginated autonomy evidence transparency acceptance proof previously listed here is no longer pending; it is part of sealed I.4.

---

## 22. Current non-claims

The repository does **not** currently claim:

- connector-introspected proof of the GitHub ruleset configuration; enforcement remains owner-confirmed from repository Settings;
- complete PostgreSQL coverage of every backend test;
- a complete transitive Python lock;
- Playwright/browser E2E coverage;
- completed god-module decomposition;
- complete future Organizational Immune System implementation beyond the closed H.2 bounded foundation;
- revision-conflict recurrence thresholds or circuit opening from revision conflicts;
- automatic retry/rebase or reuse of stale H.2.4 producer output;
- verifier-stage or G.2/G.3 revision-race attribution;
- provider/runtime health scoring or provider-wide quarantine;
- rolling-window runtime anomaly policy;
- automatic Immune System recovery;
- automatic earned-autonomy promotion;
- automatic dynamic downgrade/demotion;
- any autonomy-change command;
- a Dynamic Autonomy Manager;
- autonomy-change lineage after a real level change;
- recovery-based automatic autonomy restoration;
- generic promotion-grade evidence adapters for arbitrary capabilities;
- typed deterministic derivation of every historical I.2 measurement dimension;
- execution-attributed critical-error/recovery/SLA/incident semantics without typed linkage;
- final query-optimized I.2 index layout;
- agent self-promotion or self-grading as permission;
- provider/model-specific autonomy grants;
- a single organization-wide autonomy score;
- confidence or score as permission;
- replacement of Board ceilings or Command Gateway authority;
- production-scale operational readiness.

These non-claims are intentional. I.4 is the latest accepted **non-authorizing promotion-grade evidence foundation**. Actual autonomy mutation remains a separate future design/proof problem.

The repository **does** now claim, specifically and only within sealed I.4, accepted maximum observation/source-age policy, explicit timezone-aware deterministic `evaluation_as_of`, bounded candidate evaluation and operationally bounded/cursor-paged I.4 evidence transparency for the supported `eligibility.proposal` qualification adapter.

---

## 23. Current canonical records

Accepted architecture / history:

- `docs/GLOBAL_MOBILITY_AIOS_COMBINED_ARCHITECTURE_V1_1.md`
- `docs/HUMAN_LIKE_AGENT_ORGANIZATION_ARCHITECTURE_V1_3.md`
- `docs/GLOBAL_MOBILITY_AIOS_FINAL_COMBINED_ARCHITECTURE_V1.md` (historical predecessor)
- `docs/TECHNOLOGY_RADAR_V1_3_1.md`
- `docs/V1_3_H2_FOUNDATION_CLOSURE_AND_I1_ENTRY_2026-08-21.md`
- `docs/PLASMA_AIOS_ADOPTION_V1.md`
- `docs/CHANGELOG.md`
- `docs/V1_3_G5_ELIGIBILITY_REASSESSMENT_SUPERSESSION.md`
- `docs/V1_3_G5_ACCEPTANCE_2026-08-20.md`
- `docs/V1_3_H1_ELIGIBILITY_IMMUNE_CIRCUIT_FOUNDATION.md`
- `docs/V1_3_H1_PRODUCTION_PROOF_GATE.md`
- `docs/V1_3_H1_ACCEPTANCE_2026-08-21.md`
- `docs/V1_3_H2_1_ELIGIBILITY_WARNING_RECURRENCE_GUARD.md`
- `docs/V1_3_H2_1_ACCEPTANCE_2026-08-21.md`
- `docs/V1_3_H2_2_ELIGIBILITY_RUNTIME_HEALTH_ATTRIBUTION.md`
- `docs/V1_3_H2_2_ACCEPTANCE_2026-08-21.md`
- `docs/V1_3_H2_3_REVISION_CONFLICT_ATTRIBUTION_FOUNDATION.md`
- `docs/V1_3_H2_3_ACCEPTANCE_2026-08-21.md`
- `docs/V1_3_H2_4_POST_PRODUCER_REVISION_RACE_ATTRIBUTION.md`
- `docs/V1_3_H2_4_ACCEPTANCE_2026-08-21.md`
- `docs/V12_18_PENDING_CHANGELOG.md` (historical filename; content closed as V12.18 acceptance changelog)

Accepted H.2.2 classification-refinement records:

- `docs/V1_3_H2_2_RUNTIME_HEALTH_CLASSIFICATION_REFINEMENT_2026-08-21.md`
- `docs/V1_3_H2_2_RUNTIME_HEALTH_CLASSIFICATION_ACCEPTANCE_2026-08-21.md`

Accepted I.1 records:

- `docs/V1_3_I1_CAPABILITY_AUTONOMY_PROFILE_FOUNDATION_2026-08-21.md`
- `docs/V1_3_I1_CAPABILITY_AUTONOMY_PROFILE_ACCEPTANCE_2026-08-21.md`
- `docs/V12_20_PENDING_CHANGELOG.md` (historical filename; content closed as V12.20 I.1 acceptance changelog)

Accepted I.2 records:

- `docs/V1_3_I2_SHADOW_AUTONOMY_EVIDENCE_PROFILE_FOUNDATION_2026-08-22.md`
- `docs/V1_3_I2_SHADOW_AUTONOMY_EVIDENCE_PROFILE_ACCEPTANCE_2026-08-22.md`
- `docs/V12_21_PENDING_CHANGELOG.md` (historical filename; content closed as V12.21 I.2 acceptance changelog)

Accepted I.3 records:

- `docs/V1_3_I3_AUTONOMY_PROMOTION_ELIGIBILITY_POLICY_FOUNDATION_2026-08-22.md`
- `docs/V1_3_I3_AUTONOMY_PROMOTION_ELIGIBILITY_POLICY_ACCEPTANCE_2026-08-22.md`
- `docs/V1_3_I3_PROFILE_PRECONDITION_HARDENING_ACCEPTANCE_2026-08-22.md`
- `docs/V12_22_PENDING_CHANGELOG.md` (historical filename; content closed as V12.22 I.3 acceptance changelog)

Accepted I.4 records:

- `docs/V1_3_I4_QUALIFIED_TEMPORAL_AUTONOMY_EVIDENCE_EVALUATION_FOUNDATION_2026-08-22.md`
- `docs/V1_3_I4_QUALIFIED_TEMPORAL_AUTONOMY_EVIDENCE_EVALUATION_ACCEPTANCE_2026-08-22.md`
- `docs/V12_23_PENDING_CHANGELOG.md` (historical filename; content closed as V12.23 I.4 acceptance changelog)

Repository enforcement state:

- active `Production proof enforcement` ruleset targets `main`;
- pull request integration is required;
- branches must be up to date before merging;
- all four V12 Production Proof checks are required;
- deletions and force pushes are restricted;
- bypass list is empty;
- configuration is owner-confirmed from GitHub Settings.

The accepted technical baseline is V1.3-I.4. H.2 remains the closed bounded safety/measurement parent foundation; I.1, I.2 and I.3 remain sealed parent checkpoints. No actual promotion/demotion or Dynamic Autonomy Manager is selected or pre-authorized.

---

## 24. Current roadmap statement

Global Mobility AIOS is not changing direction away from high-autonomy architecture.

The project has converted the first four Earned Autonomy layers into repository-enforced accepted truth:

```text
I.1 — what autonomy is currently allowed for this exact capability/context
I.2 — what measured performance exists for this exact profile revision
I.3 — whether current evidence satisfies exact-profile Board-authored promotion criteria
I.4 — which evidence is canonically qualified and temporally admissible for promotion-grade evaluation
```

I.3 critic-driven review improved the architecture rather than merely changing tests: the accepted policy is bound to the exact immutable I.1 profile revision, uses valid physical `decision` Activity classification plus constitutional `AUTHORITY`, rejects non-finite rate thresholds, fails closed on profile/policy/evidence drift and has real PostgreSQL concurrency proof. The later profile-precondition hardening closed the remaining transaction-success race by serializing policy establishment with I.1 supersession on the exact profile row and rejecting supersession-wins stale commands.

I.4 then closed the concerns deliberately deferred from I.3: promotion-grade source qualification now reuses canonical domain lineage instead of trusting raw I.2 flags; temporal evaluation separates observation age from canonical source age; candidate evaluation is bounded; provenance is capped/paged separately from the summary; unsupported derivations remain unavailable; and the evaluation-policy writer carries exact-profile PostgreSQL serialization. The exact candidate passed all four Production Proof lanes.

The next risk has therefore moved forward:

> **The next risk is accidentally turning a correct evidence decision into autonomous authority without a Board-bounded, linearizable, append-only autonomy-change contract.**

The next stage is **design first**, not an implicit mutation hook. It must define, before implementation:

```text
Human Board-only command authority
exact expected current profile identity
one-level capability/context transition semantics
I.3 policy applicability requirement
I.4 promotion_grade_ready requirement
Board ceiling recheck under lock
current-profile PostgreSQL serialization
append-only new CapabilityAutonomyProfile transition
canonical autonomy-change decision Activity lineage
prior/new profile fingerprints and causation
Human override/reversal semantics
recovery semantics without history rewrite
separate promotion vs demotion rules
no agent self-promotion
no automatic invocation
real PostgreSQL races
separate exact-candidate Production Proof
```

No new stage number is invented here before the canonical architecture/design record names that increment.

The governing sequence is now:

```text
G.5 accepted baseline
→ H.1 canonical lineage + circuit foundation — SEALED
→ Production Proof + required GitHub checks — SEALED
→ H.2.1 verifier-disagreement recurrence — SEALED
→ H.2.2 trusted runtime-health attribution — SEALED
→ H.2.3 pre-egress revision-conflict attribution — SEALED
→ H.2.4 post-producer revision-race attribution — SEALED
→ H.2.2 runtime-health classification refinement — SEALED
→ H.2 bounded safety/measurement foundation — CLOSED
→ I.1 capability-specific autonomy profile/evidence — SEALED
→ I.1 exact-candidate Production Proof run 32529241957 — SEALED
→ I.2 shadow autonomy evidence profile — SEALED
→ I.2 exact-candidate Production Proof run 32533230630 — SEALED
→ I.3 exact-profile Board promotion eligibility policy — SEALED
→ I.3 exact-candidate Production Proof run 32536826352 — SEALED
→ I.3 profile-precondition hardening run 32539026789 — SEALED
→ I.4 qualified + temporal autonomy evidence evaluation — SEALED
→ I.4 exact-candidate Production Proof run 32560318311 — SEALED
→ actual capability promotion/demotion mutation design — NOT STARTED
→ autonomy-change lineage / recovery / Human override — NOT STARTED
→ implementation + separate Production Proof — NOT STARTED / NOT PRE-AUTHORIZED
→ broader Organization Fabric / operational scale
```
