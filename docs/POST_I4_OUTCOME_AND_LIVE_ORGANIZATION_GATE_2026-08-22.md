# Global Mobility AIOS — Post-I.4 Outcome + Live Organization Gate

**Date:** 2026-08-22  
**Status:** ACTIVE DEVELOPMENT GATE  
**Parent checkpoint:** V1.3-I.4 — COMPLETE / PASS / SEALED  
**I.4 technical candidate:** `46727cd130923f4ede825965cea3a011537a930b`  
**I.4 Production Proof:** `32560318311` — 4/4 PASS  
**Purpose:** convert architectural capability into demonstrated global-mobility usefulness before further major autonomy architecture expansion.

---

## 1. Development rule

> **No major architectural expansion without measurable outcome evidence.**

I.1–I.4 established strong canonical autonomy/evidence prerequisites. They do not, by themselves, prove that Global Mobility AIOS can operate a useful professional mobility organization end to end.

The next development phase therefore prioritizes measurable mobility outcomes and a real operating organization over another broad governance abstraction.

This is a sequencing correction, not an architectural reversal.

Permanent trust boundaries remain intact:

```text
memory != truth
Capability != Authority != Autonomy != Risk
CAN DO != MAY DO
ELIGIBLE != permission
scores route; deterministic gates authorize
Human Owner / Board remains supreme
external frameworks provide capability, not semantic sovereignty
```

---

## 2. Acceptance equation

Future product claims must increasingly satisfy four distinct forms of proof:

```text
Software correctness
+
Governance correctness
+
Domain correctness
+
Real-world outcome correctness
=
AIOS acceptance
```

A green software test suite is necessary but is not evidence that an immigration conclusion is correct.

A correct immigration conclusion is necessary but is not evidence that a persistent multi-agent organization can deliver it reliably, economically and transparently.

The proof layers must remain separately inspectable.

---

## 3. Immediate sequencing after I.4

```text
I.1 capability autonomy truth                         SEALED
I.2 shadow evidence measurement                      SEALED
I.3 promotion eligibility policy                     SEALED
I.4 qualified + temporal evidence evaluation         SEALED

Outcome Evaluation foundation                        PROVEN BASELINE
J.1 — Austria Agent Organization Runtime             COMPLETE / PASS / SEALED
K.1 — bounded specialist Execution/Coworker Runtime  NEXT / NOT STARTED
L — Live Organization                               NOT STARTED
Mobility gold sets + professional/shadow evaluation REQUIRED
Cost / latency / governance-overhead measurement    REQUIRED

Actual autonomy mutation                             PAUSED / NOT STARTED
Dynamic Autonomy Manager                             NOT STARTED
Automatic promotion/demotion                         NOT STARTED
```

Outcome Evaluation baseline proof:

```text
technical candidate  a89112e72f6b764d581c907809f8ed9fffdc8202
Production Proof     32563394000 — 4/4 PASS
```

J.1 acceptance proof:

```text
technical candidate       b30ae8d885f8f54285b2342a873acfe5c94ca525
Production Proof          32565671588 — 4/4 PASS
Repository Policy Check   32565671592 — PASS
SQLite                    1188 passed / 19 skipped / 0 failed
PostgreSQL 16              102 passed / 0 failed
migration head             0081_capability_autonomy_evidence_evaluation_policy
registered app tables      124
```

A later bounded autonomy-mutation increment may be reconsidered only after the project demonstrates useful real mobility work and has enough outcome evidence to justify the operational risk.

---

## 4. Outcome Evaluation programme

The evaluation layer is measurement infrastructure, not canonical legal authority.

Gold/evaluation cases must carry explicit provenance classes:

```text
SYNTHETIC
OFFICIAL_SOURCE_CURATED
PROFESSIONALLY_REVIEWED
HISTORICAL
LIVE_SHADOW
```

Provenance meanings:

- `SYNTHETIC` proves harness behavior and adversarial coverage only.
- `OFFICIAL_SOURCE_CURATED` means labels were manually curated from explicit official-source evidence and carry source provenance; it is not professional/legal review.
- `PROFESSIONALLY_REVIEWED` requires a review reference and may support stronger domain-correctness claims within the exact reviewed boundary.
- `HISTORICAL` requires traceable historical outcome/evidence provenance.
- `LIVE_SHADOW` evaluates current cases without turning benchmark labels into execution authority.

A professionally reviewed or historical case must identify its review/evidence basis and evaluation-as-of boundary. Live-shadow cases must remain non-authorizing unless a separately governed workflow permits action.

Initial mobility metrics should include explicit numerators and denominators for:

```text
pathway identification accuracy
eligibility conclusion accuracy
evidence completeness / required-evidence recall
missing-evidence detection
rule/source citation correctness
contradiction detection
professional correction rate
human interventions per case
false escalation rate
missed escalation rate
```

As trustworthy labels and completed cases become available, add:

```text
document defect rate
RFI / authority-rejection rate
successful outcome rate
time per completed case
EUR per completed case
```

No universal scalar “mobility quality score” is authorized by this gate.

---

## 5. First benchmark scope — Austria

The first benchmark remains deliberately bounded instead of claiming global coverage.

Initial target:

```text
Austria
→ selected work/residence pathway(s)
→ pathway identification
→ eligibility / insufficient-information / review-required conclusion
→ required and missing Evidence
→ official-source / VerifiedRule support
→ contradiction handling
→ governed escalation behavior
```

Existing Austria candidate-integrity, pathway-catalogue, official-source, canonical eligibility and orchestration fixtures should be reused where they represent valid software fixtures.

They are not automatically gold legal cases.

The first `OFFICIAL_SOURCE_CURATED` seed now exists at:

```text
apps/api/evaluations/mobility_cases/austria_rwr_shortage_2026_v1.json
```

It is scoped to the 2026 Austrian Red-White-Red Card route for Skilled Workers in Shortage Occupations and currently contains three source-curated synthetic-person fact patterns. The dataset explicitly records `professional_review_status = NOT_REVIEWED`.

This seed is meaningful domain-evidence progress, but a serious domain benchmark still requires broader curated coverage and, where appropriate, professional review/historical/live-shadow validation.

---

## 6. J → K → L organization proof

The canonical V1.3 sequence remains controlling.

### J.1 — Austria Agent Organization Runtime — ACCEPTED

Acceptance record:

```text
docs/V1_3_J1_AUSTRIA_MOBILITY_OBJECTIVE_RUNTIME_ACCEPTANCE_2026-08-22.md
```

J.1 proved the first bounded native organization topology by reusing existing primitives rather than introducing a new Mission store or agent framework.

Accepted topology:

```text
Austria mobility objective
→ mobility_operations_lead
   ├─ pathway_operations_specialist WorkItem
   └─ regulatory_intelligence_analyst WorkItem
→ fresh canonical ContextBundles
→ provider-neutral EmployeeRuntimeBindings
→ structural specialist-completion gate
→ owner synthesis readiness
```

Accepted J.1 properties include:

```text
persistent position ownership
exact-replay objective creation
no duplicate objective topology
existing OrganizationActivity lineage
fresh ContextBundle per specialist WorkItem
runtime/provider identity does not grant authority
tool availability remains bounded by Context Authority
stale ContextBundle runtime rebinding fails closed
owner synthesis blocked until both required specialist WorkItems complete
```

J.1 does not execute the specialist work. It does not create legal conclusions, authoritative Contributions or durable specialist action outputs. That boundary remains K.1.

### K.1 — bounded Execution / Coworker Runtime — NEXT

K.1 should add only the bounded execution capabilities needed by the selected Austria mobility slice, behind AIOS-owned adapters and existing authority controls.

K.1 must reuse J.1 WorkItems and current canonical ContextBundles and should prefer existing execution/output primitives such as:

```text
OrganizationExecutionAttempt
OrganizationalActionOutput
OrganizationActivity
controlled/native AIOS execution services
```

Initial K.1 proof should require:

```text
two required specialist WorkItems execute from current canonical context
one durable current-work output per required specialist
output provenance bound to exact WorkItem/context/runtime identity
exact replay / idempotency
stale context fails closed
provider/model identity remains non-authorizing
no external side effects in the initial slice
owner synthesis requires current durable specialist execution evidence
latency / retry / governance measurement hooks
```

Do not add a new agent framework merely to satisfy K. Candidate runtimes/donor patterns must show a measured gap versus native AIOS execution.

### L — Live Organization

L requires **real runtime organizational activity, not simulated dashboard data**.

The proof should expose:

```text
Mission/objective
agents / persistent positions
WorkItems
collaboration
blocked work
material decisions
Evidence / rule lineage
tool activity
incidents / escalation
autonomy state
cost
latency
quality / outcome metrics
```

in the Global Mobility AIOS Cockpit / transparency surfaces.

---

## 7. Governance overhead budget

Governance must prove that it enables safe autonomy rather than becoming bureaucracy.

Adopt the Governance Amplification Factor (GAF):

```text
GAF = governed completion cost / minimally viable raw task cost
```

GAF must be reported by risk tier and workflow. A single organization-wide value may hide excessive overhead in R0/R1 work.

Cost attribution should distinguish where measurable:

```text
primary inference
Context Broker assembly
independent verification
deterministic governance
execution/tools
retry + stale-work loss
observability/storage
human/professional intervention
```

Target principle remains:

> **Governance Cost ∝ Risk × Uncertainty × Novelty**

R0/R1 should stay close to the minimally governed path. R4/R5 may justify materially higher amplification.

---

## 8. Latency and intervention budgets

Measure at minimum:

```text
p50 / p95 workflow latency by risk tier
p50 / p95 material-action governance latency
verification calls per completed workflow
stale / retry rate
human interventions per case
Board interventions per 1,000 organizational actions
false escalation rate
missed escalation rate
governance latency share
governance cost share
```

Budgets should be calibrated from measured workflows rather than invented globally before data exists.

---

## 9. Context Broker adversarial proof

Before broad Live Organization dependence, Context Broker evaluation must intentionally test:

```text
mandatory Evidence omitted
stale rule/source supplied
known contradiction omitted
malicious or instruction-bearing retrieved content
cross-case contamination
cross-tenant contamination
outdated conversation/decision summary
missing authority/autonomy/risk constraints
irrelevant context flooding
```

Desired behavior is governed uncertainty, blocking, refresh or escalation where required.

The benchmark must not reward a model for improvising around missing canonical truth.

Retrieved knowledge remains data, not executable authority.

---

## 10. Recovery and irreversible workflow proof

Move recovery proof from generic contracts into selected mobility workflows.

For each material action, classify the consequence as:

```text
REVERSIBLE
COMPENSATABLE
IRREVERSIBLE
APPEND_ONLY_CORRECTION
```

The first live slices should prove at least one compensation/recovery path and one pre-execution protection for an irreversible or externally consequential action before broad external autonomy is considered.

---

## 11. Donor / framework evidence rule

Vendoring is not adoption and architecture resemblance is not value.

For Munder, Plasma or any future donor/framework, adoption beyond a bounded pilot requires a measured comparison against the native AIOS baseline on the same task, including relevant dimensions such as:

```text
correctness
latency
cost
operational complexity
failure behavior
security / authority compatibility
transparency / lineage completeness
maintainability
```

No new major framework is added by default without a measured architectural gap.

---

## 12. Product / UX boundary

Internal architecture belongs primarily in Cockpit and engineering/transparency surfaces.

`My Mobility` should optimize for the user’s goal:

```text
What can I do?
What am I eligible for?
What is missing?
What should happen next?
What do I need to provide or decide?
```

Users should not need to understand WorkItem internals, A-level terminology, policy fingerprints or lineage graphs to complete a mobility journey.

---

## 13. Documentation/current-state reconciliation

Plasma PR #7 has been merged into the V12 branch at merge commit `33425eb2b465b2dc14d9e43c556457e5135b626a`. Current-state documentation must no longer describe it as an open draft/source-import-incomplete PR.

Historical dated changelog statements remain historical facts and should not be rewritten. New current-state records supersede them explicitly.

Vendoring remains distinct from production adoption.

---

## 14. Exit criteria for this gate

This gate is not satisfied by creating evaluation schemas alone.

Before another major autonomy architecture expansion, the repository should show evidence of:

1. a versioned mobility evaluation harness with explicit case provenance and denominator-safe metrics;
2. an initial official-source-curated Austria benchmark beyond synthetic harness-only tests;
3. broader/professionally reviewed, historical or live-shadow validation sufficient for the claims being made;
4. at least one real J→K→L vertical where persistent AI employees cooperate on mobility work;
5. domain-correctness results on that vertical;
6. cost, latency and governance-overhead measurements;
7. Context Broker adversarial results;
8. at least one workflow-level recovery/irreversibility proof;
9. Cockpit visibility into the real organization activity;
10. clear My Mobility UX that does not expose internal architecture unnecessarily;
11. any donor/framework expansion justified by benchmark evidence rather than availability.

Current progress satisfies items 1 and 2 at the stated non-professional benchmark boundary and establishes the J.1 organization-runtime prerequisite for item 4. It does not yet satisfy the full J→K→L vertical.

The first iterations may be small. The key requirement is that claimed usefulness is measured rather than inferred from architectural sophistication.

---

## 15. Non-claims

This gate does **not** claim:

- current professional immigration accuracy;
- a professionally validated Austria gold set;
- live-case outcome validation;
- K.1 bounded specialist execution acceptance;
- a complete Live Organization;
- donor production adoption;
- bounded autonomy mutation;
- automatic promotion or demotion;
- complete cost/latency calibration;
- production-scale commercial validation.

Those are outcomes to prove, not conclusions to pre-write.
