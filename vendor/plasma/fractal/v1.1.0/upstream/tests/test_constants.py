"""Test the ``fractal.constants`` module.

Pure-constant module with no behavior of its own; the path names and the
status/event/signal vocabularies are pinned end-to-end by the suites that
consume them (node lifecycle, CLI, and TUI tests).
"""

from __future__ import annotations

__all__ = []
