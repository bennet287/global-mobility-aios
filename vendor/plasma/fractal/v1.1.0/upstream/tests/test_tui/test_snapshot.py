"""Test the ``fractal.tui.snapshot`` module.

The snapshot builder is pinned end-to-end by ``test_data``, whose tests
read the canonical tree exclusively through ``builder.build(scope)`` and
assert the shaped snapshots; this placeholder completes the layout mirror.
"""

from __future__ import annotations

__all__ = []
